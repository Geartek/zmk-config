# Enable deep sleep support
CONFIG_ZMK_SLEEP=y
# Milliseconds of inactivity before entering idle state (5 minutes)
CONFIG_ZMK_IDLE_TIMEOUT=300000
# Milliseconds of inactivity before entering deep sleep (15 minutes)
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=900000
# Increase the transmit power of the keyboard's BLE radio
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y
# BLE Stability Improvements
CONFIG_ZMK_BLE_EXPERIMENTAL_CONN=y

# Uncomment the following line to enable the Corne OLED Display
CONFIG_ZMK_DISPLAY=y
# nice_nano_v2's EXT_POWER node (P0.13) is enabled by its own driver at boot
# regardless of power source, but only when this is =y. On this board the
# left OLED's VCC runs off that switched rail, so leaving this =n meant the
# left screen stayed dark whenever running on battery instead of USB.
CONFIG_ZMK_EXT_POWER=y
CONFIG_ZMK_WIDGET_OUTPUT_STATUS=y
CONFIG_ZMK_WIDGET_LAYER_STATUS=y
CONFIG_ZMK_WIDGET_WPM_STATUS=y
CONFIG_ZMK_WIDGET_BATTERY_STATUS=y
CONFIG_ZMK_WIDGET_BATTERY_STATUS_SHOW_PERCENTAGE=y
# The peripheral (right) side has no output/layer/wpm widgets of its own;
# this keeps its old built-in-screen behaviour (just battery + BT status).
CONFIG_ZMK_WIDGET_PERIPHERAL_STATUS=y

# Custom status screen (src/status_screen.c), same widgets/layout as the
# built-in screen.
CONFIG_ZMK_DISPLAY_STATUS_SCREEN_CUSTOM=y
# Choosing _CUSTOM over _BUILT_IN also skips the font selection the built-in
# status screen implies (see zmk/app/src/display/Kconfig), which left the
# output/layer/wpm widgets without the glyphs their icons need. Re-add it
# explicitly so those icons render instead of "unknown glyph" boxes.
CONFIG_LV_FONT_MONTSERRAT_16=y
CONFIG_LV_FONT_MONTSERRAT_12=y
CONFIG_LV_FONT_DEFAULT_MONTSERRAT_16=y

# Uncomment the following lines to enable the Corne RGB Underglow
# CONFIG_ZMK_RGB_UNDERGLOW=y
# CONFIG_WS2812_STRIP=y
